---
interface Props {
  href: string;
  class?: string;
}
const { href, class: className = '' } = Astro.props;
---

<a
  href={href}
  class:list={[
    'underline-fill pb-0.5 text-[15px] font-semibold text-dark no-underline',
    className,
  ]}
>
  <slot />
</a>
